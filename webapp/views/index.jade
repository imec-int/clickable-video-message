doctype html5
html
	head
		title=title
		meta(charset='utf-8')
		meta(name='viewport', content='user-scalable=no, initial-scale=1.0, maximum-scale=1.0, width=device-width')
		meta(name='apple-mobile-web-app-capable', content='yes')
		link(rel='stylesheet', href='stylesheets/style.css')

		script(type='text/javascript', src='/vendor/jquery-2.1.1.min.js')
		script(type='text/javascript', src="/vendor/fastclick.js")

		script(type='text/javascript', src='/javascripts/index.js')

		script.
			$(function(){
				var app = new App({
					person: !{JSON.stringify(person)}
				});
				app.init();
			});
	body
		script.
			var _gaq = _gaq || [];
			_gaq.push(['_setAccount', 'UA-52545961-1']);
			_gaq.push(['_trackPageview']);

			(function() {
				var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
				ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
				var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
			})();


		.content
			video(poster='/video/poster.png', preload="auto")
				source(src='#{webm}', type='video/webm')
				source(src='#{ogg}', type='video/ogg')
				source(src='#{mp4}', type='video/mp4')


			.overlay
				//- only needed on iPhone/iPod:
				img.endimage(src='/video/end.png')

				.response.thankyou
					p
						| Alvast bedankt.
						br
						| Studio media gaat door op 23 oktober (namiddag).
						br
						| Je ontvangt dadelijk een bevestigingsmail.
				.response.toobad
					p
						| Jammer.
						br
						| Maar zeker tot een volgende gelegenheid!

				.yesimage

				img.spinner.rotating(src='/images/spinner.png')
				img.play(style="display:none", src='/images/playbutton.png')


				.choice
					.yes Ik wil er graag bij zijn.
					.no Dit lukt jammer genoeg niet.








